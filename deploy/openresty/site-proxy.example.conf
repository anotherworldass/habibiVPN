# 把 1Panel 站点「反向代理」原文里的 proxy_pass 改成对应 upstream 名。
# 不要写死 127.0.0.1:3000，否则蓝绿切换不会生效。
#
# 用户站 www：
#   proxy_pass http://habibi_web;
# 独立 API 域名（可选）：
#   proxy_pass http://habibi_api;
# Telegram Mini App：
#   proxy_pass http://habibi_tg;
# 管理后台：
#   proxy_pass http://habibi_admin;
#
# 其余 proxy_set_header / SSL 保持 1Panel 原样。

location / {
    proxy_pass http://habibi_web;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
}
